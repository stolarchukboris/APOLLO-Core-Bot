{
  "name": "cca",
  "version": "1.0.0",
  "description": "",
  "main": "build/src/index.js",
  "type": "module",
  "scripts": {
    "processStart": "pm2 start build/src/index.js --name \"APOLLO Core Bot\" && pm2 logs",
    "processRestart": "pm2 restart build/src/index.js && pm2 logs",
    "start": "tsc && pnpm run processStart",
    "deploy-start": "tsc && pnpm run process -- --deploy",
    "restart": "tsc && pnpm run processRestart",
    "deploy-restart": "tsc && pnpm run processRestart -- --deploy",
    "stop": "pm2 delete build/src/index.js",
    "test": "tsc && node . --deploy --nologin"
  },
  "keywords": [],
  "author": "THE_BESTol0ch",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^14.24.1",
    "dotenv": "^17.2.3",
    "ms": "^2.1.3"
  },
  "devDependencies": {
    "@types/ms": "^2.1.0"
  }
}